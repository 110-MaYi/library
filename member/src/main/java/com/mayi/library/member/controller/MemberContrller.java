package com.mayi.library.member.controller;import com.mayi.library.member.entity.Member;import com.mayi.library.member.service.MemberService;import org.springframework.stereotype.Controller;import org.springframework.web.bind.annotation.CrossOrigin;import org.springframework.web.bind.annotation.GetMapping;import org.springframework.web.bind.annotation.ResponseBody;import javax.annotation.Resource;import java.util.HashMap;import java.util.Map;/** * @author mayi * @create 2018-08-25 下午9:00 */@Controller@CrossOriginpublic class MemberContrller {    @Resource    private MemberService memberService;    @GetMapping("/check")//映射，在URL中输入相当于地址    @ResponseBody    public Map checkMobile (String mobile){        Map result = new HashMap();        try {            //当返回成功时            result.put("code" , "0");            result.put("message","success");            Member member = memberService.checkByMobile(mobile);            result.put("data",member);        }catch (Exception e){            //失败时            //打印出日志            e.printStackTrace();            //将错误的类名作为code返回            result.put("code",e.getClass().getSimpleName());            result.put("message",e.getMessage());        }        return result;    }}