package com.mayi.library.member.service;import ch.qos.logback.core.joran.conditional.IfAction;import com.mayi.library.member.entity.Member;import com.mayi.library.member.repository.MemberRepository;import com.mayi.library.member.service.exception.MemberNotFoundException;import org.springframework.stereotype.Service;import javax.annotation.Resource;import java.util.List;/** * @author mayi * @create 2018-08-25 下午8:38 */@Servicepublic class MemberService {    @Resource    //注入动态生成的MemberRepository    private MemberRepository memberRepository;    //书写方法    public Member checkByMobile(String mobile){        List<Member> members = memberRepository.findByMobile(mobile);        //对反悔的members做一个判断是否为空        if (members.size() == 0) {            throw new MemberNotFoundException("会员不存在");        }        //用户存在        return members.get(0);    }}